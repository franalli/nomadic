[tool.black]
line-length =100
target-version = ["py312"]
include = "app/"

[tool.ruff]
line-length = 100
target-version = "py312"
exclude = ["alembic"]

[tool.ruff.lint]
# Errors, flake8, imports, bugbear
select = ["E", "F", "I", "B"]
ignore = []

[tool.ruff.lint.isort]
known-first-party = ["app"]
