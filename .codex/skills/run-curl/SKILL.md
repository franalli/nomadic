---
name: run-curl
description: Mirror of `.claude/commands/run-curl.md` to execute backend curl flow suites and produce an issue report.
metadata:
  source_command: .claude/commands/run-curl.md
---

# run-curl

Use this skill as a 1:1 mapping to `.claude/commands/run-curl.md`.

## Instructions

1. Open `.claude/commands/run-curl.md`.
2. Execute all prerequisite checks and both test suites exactly as defined.
3. Produce the structured report format and perform teardown exactly as instructed.
